odb.applydeftemplate/002-bad_def # fails randomly on Nix/aarch64-darwin
# errors need updating
openroad.detailedrouting/004-fail-range-max
openroad.globalplacement/005-fail-util
openroad.globalplacement/002-fail-density
openroad.detailedrouting/003-fail-range-min
# DRT_THREADS recently deprecated
openroad.detailedrouting/006-success-threads-single
openroad.detailedrouting/007-success-threads-multiple
# netgen 1.5.320 (nixpkgs) no longer removes a pinless placeholder cell via
# 'flatten class': the class survives into the compare as an unmatched
# instance. Real flatten scenarios (cells with pins) are unaffected --
# 003-fail-no-flatten still exercises the mismatch path.
netgen.lvs/002-success-flatten
# OpenROAD PSM (2026) cannot trace the met1 plates of the 2022-era
# sky130_ef_sc_hd__decap_12 endcaps these frozen DEFs carry, reporting ~500
# unconnected shapes/instances per net on a grid pdngen built correctly.
# Fresh floorplans pass the same deferred gate (CI design flows, sky130 and
# gf180); the failure-mode cases 003-007 still exercise the check.
openroad.generatepdn/001-success-default
openroad.generatepdn/002-success-custom
